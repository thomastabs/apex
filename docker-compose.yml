services:
  apex:
    build: .
    ports:
      - "3000:3000"
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist the contextspec/ context files across container restarts
      - ./contextspec:/app/contextspec
    restart: unless-stopped
