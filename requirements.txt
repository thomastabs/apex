# Pinned to the verified-working set (test suite green on these versions).
# ~= allows patch updates only; bump minors deliberately and re-run tests.
fastapi~=0.136.1
uvicorn[standard]~=0.47.0
langchain~=1.3.1
langchain-core~=1.4.0
langchain-anthropic~=1.4.3
langchain-openai~=1.2.2
langchain-google-genai~=4.2.3
openai~=2.38.0
anthropic~=0.96.0
requests~=2.32.3
httpx~=0.28.1
python-dotenv~=1.0.1
pydantic~=2.13.3
azure-storage-file-share~=12.25.0
# Encrypts saved per-account AI provider keys at rest (src/ai_key_store.py).
# Already pulled in transitively by langchain-google-genai -> google-auth;
# pinned directly here because we now import it ourselves.
cryptography~=49.0
# Optional — only imported when REDIS_URL is set (multi-replica coordination via
# Upstash). Unset = single-replica, redis is never touched. See src/distributed.
redis~=5.2.1
